import 'package:flutter/material.dart';

class RadioButtonPage extends StatelessWidget {
  const RadioButtonPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("RadioButtonPage"),
        centerTitle: true,
      ),
    );
  }
}